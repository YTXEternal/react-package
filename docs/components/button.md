# Button 按钮

常用的操作按钮。

## 基础用法

```tsx
import { Button } from 'ux-table-react'

export default () => <Button label="基础按钮" />
```

## API

| 属性名 | 说明 | 类型 | 默认值 |
| --- | --- | --- | --- |
| label | 按钮文本 | `string` | - |
| className | 自定义类名 | `string` | - |
